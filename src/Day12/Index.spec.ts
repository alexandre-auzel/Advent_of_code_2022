import { day12 } from ".";

describe("", () => {
  test("", () => {
    // GIVEN

    // WHEN
    const actual = day12();

    // THEN
    const expected: number = 1;
    expect(actual).toEqual(expected);
  });
});
