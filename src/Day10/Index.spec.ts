import { day10 } from ".";

describe("", () => {
  test("", () => {
    // GIVEN

    // WHEN
    const actual = day10();

    // THEN
    const expected: number = 1;
    expect(actual).toEqual(expected);
  });
});
