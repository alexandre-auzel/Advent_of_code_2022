import { day4 } from ".";

describe("", () => {
  test("", () => {
    // GIVEN

    // WHEN
    const actual = day4();
    console.log(actual);

    // THEN
    const expected: number = 1;
    expect(actual).toEqual(expected);
  });
});
