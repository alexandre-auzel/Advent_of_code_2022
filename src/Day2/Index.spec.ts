import { day2 } from ".";

describe("", () => {
  test("", () => {
    // GIVEN

    // WHEN
    const actual = day2();

    // THEN
    const expected: number = 0;
    expect(actual).toEqual(expected);
  });
});
