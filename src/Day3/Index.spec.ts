import { computePriority, day3, day3_bis } from ".";

describe("", () => {
  test("", () => {
    // GIVEN

    // WHEN
    const actual = computePriority("a");

    // THEN
    const expected: number = 1;
    expect(actual).toEqual(expected);
  });

  test("", () => {
    // GIVEN

    // WHEN
    const actual = computePriority("B");

    // THEN
    const expected: number = 28;
    expect(actual).toEqual(expected);
  });
  test("", () => {
    // GIVEN

    // WHEN
    const actual = day3();

    // THEN
    const expected: number = 0;
    expect(actual).toEqual(expected);
  });

  test("", () => {
    // GIVEN

    // WHEN
    const actual = day3_bis();

    // THEN
    const expected: number = 0;
    expect(actual).toEqual(expected);
  });
});
