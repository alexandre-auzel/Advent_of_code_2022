import { day9 } from ".";

describe("", () => {
  test("", () => {
    // GIVEN

    // WHEN
    const actual = day9();

    // THEN
    const expected: number = 1;
    expect(actual).toEqual(expected);
  });
});
