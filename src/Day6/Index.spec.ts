import { day6 } from "../Day6";

describe("", () => {
  test("", () => {
    // GIVEN

    // WHEN
    const actual = day6();
    console.log(actual);

    // THEN
    const expected: number = 1;
    expect(actual).toEqual(expected);
  });
});
